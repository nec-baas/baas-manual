.. include:: ../common/linkdef.rst

==================
イントロダクション
==================

本文書の位置づけ
================

本文書は、SSE Push サーバの運用者向けのドキュメントです。
本文書では、以下の内容について説明します。

* SSE Push サーバのインストール手順
* SSE Push サーバのコンフィグレーション手順

SSE Push サーバについて
========================

SSE Push サーバは、HTTP の SSE (`Server Sent Event`_) 仕様に基づく Push 配信サーバです。
`Server Sent Event`_ は、サーバから任意のタイミングでクライアントに通知を送信することができるプロトコルです。

iOS APNs や Android GCM/FCM とは異なり、独自に設置した Push サーバからクライアントに Push 配信を行うことができます。
このため、閉域網内で Push 配信を行うことが可能です。

SSE Push サーバはモバイルバックエンド基盤サーバと共に運用することを想定しています。
(SSE Push サーバのみを単独で運用することも可能ですが、この場合は別途フロントエンドサーバを開発する必要があります)
