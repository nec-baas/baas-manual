HTTPS の使用
============

SAML 認証を使用する場合は、BaaS API サーバは必ず HTTPS 接続を提供していなければ
なりません。暗号化されていない HTTP 接続では SAML 認証が失敗します。

HTTPS 接続を提供する場合には SSL 証明書の設定などが必要になります。
詳細は `サーバ利用手順書 <../../server/index.html>`_ を参照してください。

また、デベロッパーコンソールのシステム設定で、APIサーバベース URI に https スキームの URI が設定
されていることを確認してください。
